import { Cinzel, Fauna_One, Inter } from "next/font/google";
import { Aref_Ruqaa_Ink } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const arefRuqaa = Aref_Ruqaa_Ink({subsets: ['latin'], weight:['400', '700']});
export const cinzel = Cinzel({subsets:['latin']})
export const faunaOne = Fauna_One({subsets:['latin'], weight:['400']})